import Testing
@testable import LocusKit

/// Reachability sentinel — confirms the test target compiles against
/// the LocusKit module. Real coverage lives in the per-type test files
/// added in Part 5.
@Suite("PackageBuildTests")
struct PackageBuildTests {
}

/// LocusKit — the MemPalace surface for MOOTx01.
///
/// This module provides the MemPalace spatial primitives: drawers
/// (verbatim content), wings and rooms (metadata-only taxonomy),
/// tunnels (typed cross-references), and diary entries. LocusKit
/// depends on SubstrateLib, SubstrateKernel, SubstrateML, and
/// PersistenceKit. GeniusLocusKit composes on top of LocusKit —
/// not the other way around.
///
/// This file is intentionally minimal — the public types live in
/// neighbouring source files (Drawer, Tunnel, DiaryEntry,
/// Summaries, LocusKitError, DrawerStore).

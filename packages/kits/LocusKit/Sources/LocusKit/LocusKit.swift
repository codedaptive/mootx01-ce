/// LocusKit — the MemPalace surface for MOOTx01.
///
/// This module wraps the temporal knowledge-graph layer in
/// GeniusLocusKit with the spatial primitives MemPalace exposes:
/// drawers (verbatim content), wings and rooms (metadata-only
/// taxonomy), tunnels (typed cross-references), and diary entries.
/// LocusKit composes on top of GeniusLocusKit; it does not inherit.
///
/// This file is intentionally minimal — the public types live in
/// neighbouring source files (Drawer, Tunnel, DiaryEntry,
/// Summaries, LocusKitError, DrawerStore).

[package]
name = "persistence-kit"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Storage abstraction layer for the GeniusLocus substrate kits. Closed-enum predicate algebra, Storage + RowStore + BlobStore + VectorIndex + AuditLog + StorageObserver traits. InMemory backend ships at v1.0; SQLite backend is a follow-on."
license = "MIT OR Apache-2.0"
publish = false

[lib]
name = "persistence_kit"
path = "src/lib.rs"

[dependencies]
substrate-types = { path = "../../../libs/SubstrateTypes/rust" }
uuid = { version = "1", features = ["v4"] }
# SQLite backend: bundled compiles SQLite from source (no system dep).
rusqlite = { version = "0.31", features = ["bundled"] }
# ISO-8601 timestamp formatting for TEXT date columns (schema invariant).
chrono = "0.4"
# PostgreSQL backend: synchronous client (matches the sync Storage trait).
# Native-type features map UUID/TIMESTAMPTZ columns to uuid::Uuid /
# chrono::DateTime<Utc>.
postgres = { version = "0.19", features = ["with-uuid-1", "with-chrono-0_4"] }

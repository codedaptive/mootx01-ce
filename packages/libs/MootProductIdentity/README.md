# MootProductIdentity

The product identity constants, spelled once: the Application Support folder name, the logging subsystem, and the Apple bundle identifiers. Swift and Rust, pinned to `Fixtures/product_identity.json`.

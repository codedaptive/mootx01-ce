# Roo Code MOOTx01 Adapter

Roo Code supports workspace rules in `.roo/rules/` and a fallback `.roorules`
file. This adapter includes both.

Copy `.roo/` into a project root. Use `.roorules` only for fallback
compatibility.


# Cline MOOTx01 Adapter

Cline reads workspace rules from `.clinerules/` and can also detect AGENTS.md,
Cursor, and Windsurf rule files. This adapter uses the native `.clinerules/`
directory.

Copy `.clinerules/` into a project root.


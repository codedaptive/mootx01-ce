# Continue MCP server snippet for MOOTx01.
# Lives at ~/.continue/mcpServers/mootx01.yaml. Continue reads
# every YAML file in that directory; restart Continue after the
# installer runs.
name: mootx01
version: 0.1.0
schema: v1
mcpServers:
  - name: mootx01
    command: /ABSOLUTE/PATH/TO/.local/share/MOOTx01/bin/mootx01-mcp
    args: []

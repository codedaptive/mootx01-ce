{
  "_comment": "Cursor MCP config snippet. Lives at ~/.cursor/mcp.json. Cursor reads this on startup; restart Cursor after install.sh runs.",
  "mcpServers": {
    "mootx01": {
      "command": "/ABSOLUTE/PATH/TO/.local/share/MOOTx01/bin/mootx01-mcp",
      "args": [],
      "env": {}
    }
  }
}

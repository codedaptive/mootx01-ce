# Cursor MOOTx01 Adapter

Cursor rules live in `.cursor/rules/*.mdc`. This adapter also includes a
legacy `.cursorrules` file for older setups.

Copy `.cursor/` into a repository root. Use `.cursorrules` only when you need
legacy compatibility.


# AI Knowledge for Benchmark Documentation

These are final release 1.1 specifications, not project reports. The run book
is authoritative for operation; protocol/spec documents define semantics;
method defines measurement and reporting; estates defines artifact lifecycle;
benchmark identity and origin live in `BENCHMARKS_USED.md`.

Write from the premise that the documented release exists. Do not add status,
delta-to-finish, implementation history, placeholders, agent dialogue, private
paths, or direct commands that bypass the Makefile. A code disagreement is an
implementation defect unless the release owner changes the specification.

---
title: CVK-ICLOUD Tracked Follow-Ups
version: v0.1
status: active
date: 2026-07-17
description: "Items deferred from the CVK-ICLOUD program, each with file location and suggested scope."
---

# CVK-ICLOUD Tracked Follow-Ups

Items deferred from the 30-mission CVK-ICLOUD program. Each entry
names what, why it was deferred, where it lives, and a suggested scope
for the follow-up mission.

| # | Item | What | Why deferred | File / location | Suggested scope |
|---|---|---|---|---|---|
| 1 | ~~**Tier-rise retraction (Perkins A2)**~~ **DONE (CVK-WB1)** | **Shipped CVK-WB1:** `SensitivityFilteredObserver` emits a retraction tombstone (synthetic delete, `origin: .local`, `values: nil`) for above-ceiling UPDATE events. `SensitivityFilteredRowStore.deleteSync` blocks self-delivered tombstones to preserve the local restricted row. Peers hard-delete their snapshot via the normal tombstone path. Demotion back below-ceiling re-syncs naturally on the next local write. Seven deterministic tests added. | Completed 2026-07-17. | `apps/Mootx01-App/Sources/MootGateway/Sync/SensitivityFilteredStorage.swift` | — |
| 2 | ~~**In-app sync toggle (Simms verdict 1)**~~ **DONE (CVK-WB2)** | **Shipped CVK-WB2:** `SyncPolicy` (UserDefaults-backed) + toggle in `SyncTileView` (EngineView) + startup `configure()` call in `Mootx01App`; user guide updated. | Completed 2026-07-17. | `apps/Mootx01-App/Sources/GatewayUI/EngineView.swift`, `Sources/MootGateway/Sync/SyncPolicy.swift` | — |
| 3 | ~~**CKRecord.encryptedValues eval**~~ **EVALUATED (CVK-WB3) — DEFER** | **CVK-WB3:** Full type-compatibility confirmed (all TypedValue cases supported by encryptedValues). Zone-feed pull path (`fetchZoneChanges`) makes the server-side no-query restriction a nil impact. Metadata fields (`_sync*`) must stay plaintext. Slot/registry records must stay plaintext. Migration sketch: dual-read decode + `SyncManifest.encryptedContentColumns` opt-in (no SideSchema version bump needed). Recommendation: **DEFER** until a named content sensitivity requirement (e.g. diary body) triggers adoption. Dashboard debuggability cost + non-trivial migration are unjustified without a named requirement; ADR-014 SQLCipher covers at-rest. Bob decides. | Evaluated 2026-07-17. | `docs/decisions/DECISION_CONVERGENCEKIT_ENCRYPTEDVALUES_2026-07-17.md` | — (decision record is the output; no code unless Bob approves adoption) |
| 4 | ~~**TableChange.changedColumns (PersistenceKit)**~~ **DONE — CVK-WB4** | `changedColumns: Set<String>?` added to `TableChange` (both legs). InMemory and SQLite backends stamp changed columns on insert/update/upsert; delete emits nil. ConvergenceKit uses changedColumns for (a) mixed-column storm-kill precision (Scorandum Q1 closed) and (b) fieldLevelLWW stamp precision. Rust twin: `changed_columns: Option<HashSet<String>>`. All 10 Swift tests pass; 5 Rust tests pass. Docs updated: PERSISTENCEKIT_SPEC.md B-20, PERSISTENCEKIT_INTERFACE.md TableChange block, CONVERGENCEKIT_SPEC.md B-8 + B-14. | — | — | — |
| 5 | ~~**Outbox secondary index + readSyncHLC batch-read**~~ **DONE (CVK-WB5)** | **Shipped CVK-WB5:** composite index `idx_ck_outbox_table_row` on `_ck_outbox(table_name,row_key)` (CKSideSchema v8) + `SyncMetaStore.readSyncHLCs(batch:)` pre-load in `PullCycle` with per-row fallback. ColumnHLCStore batch read noted as non-trivial follow-on in the WB5 report. | Completed 2026-07-17. | `packages/kits/ConvergenceKit/Sources/ConvergenceKit/Outbox/OutboxStore.swift`; `packages/kits/ConvergenceKit/Sources/ConvergenceKitCloudKit/CKSideSchema.swift` | Primitive-touching mission: add index in `SideSchema` migration at next version bump; add `readSyncHLCBatch(rowKeys:)` to `CKSyncMetaStore`. Must carry tests for both. |
| 6 | ~~**Scheduler retryableBackoff wiring**~~ | **DONE (CVK-WB6).** `AdaptivePollScheduler` now classifies pull errors via `CKErrorTaxonomy` and applies `RetryPolicy` exponential backoff (injected policy + jitter source). Effective sleep = `max(tier interval, backoffFloor)`. `retryAfterSeconds` is honoured as a hard floor. Success resets attempt counter; non-retryable errors leave tier cadence unchanged. Five deterministic tests added. | Completed 2026-07-17. | `packages/kits/ConvergenceKit/Sources/ConvergenceKitCloudKit/Engine/AdaptivePollScheduler.swift` | — |
| 7 | ~~**TombstoneGC production scheduling**~~ **DONE (CVK-WB7)** | **Shipped CVK-WB7:** `TombstoneGC.compact` is now scheduled in the convergence loop. CloudKit: `AdaptivePollScheduler` gained a `gcFn: SchedulerGCFn?` injection point and a `nowMs` clock injection; after each successful pull, the closure calls `CloudKitStateActor.gcIfDue(nowMs:)`, which reads the last-GC wall time from a sentinel row in `_ck_change_token` (zone_name=`_gc_tombstone_sweep`), runs `TombstoneGC.compact` for `_ck_sync_meta` if 24 h has elapsed, and persists the new timestamp. Federation: `FederationStateActor.pull()` calls `gcIfDue(nowMs: nowMillis())` after each cycle; sentinel stored in `_fed_sync_meta` (table_name=`_gc_state`). Six deterministic tests added (3 CloudKit, 3 Federation): compact runs when due, skipped when not due, entries inside retention window survive. Critical invariant (gcRetentionSeconds must exceed P1-M3 slot-eviction window) commented at both seams. | Completed 2026-07-17. | `packages/kits/ConvergenceKit/Sources/ConvergenceKitCloudKit/Engine/TombstoneGCCoordinator.swift`; `AdaptivePollScheduler.swift`; `FederationSyncEngine.swift`; `TombstoneGCSchedule.swift` | — |
| 8 | ~~**Benchmark lane placement**~~ **DONE (CVK-WB8)** | **Shipped CVK-WB8:** Four perf suites (Q1/Q2/Q3/Q5) in `CVK_ICLOUD_P4M5_PerfTests.swift` gated behind `.enabled(if: ProcessInfo.processInfo.environment["MOOT_PERF_BENCH"] == "1", ...)` on each `@Suite`. Default `swift test` skips them (CloudKit bundle returns to seconds); `MOOT_PERF_BENCH=1 swift test --no-parallel --filter ...` or `make test-perf-bench` runs the full benchmarks. All suites already `.serialized`; no Package.swift change needed — the `.enabled(if:)` trait handles gating in-process, following the GLK_LATENCY_TESTS convention. | Completed 2026-07-17. | `packages/kits/ConvergenceKit/Tests/ConvergenceKitCloudKitTests/CVK_ICLOUD_P4M5_PerfTests.swift`; `scripts/moot-test`; `Makefile` | — |
| 9 | ~~**Rust fieldLevelLWW column-grain parity**~~ **DONE (CVK-WB9)** | **Shipped CVK-WB9:** `_fed_sync_meta_cols` side table added to the Rust Federation schema (version 2); `field_lww_merge` and `tombstone_wins` pure functions ported from Swift `FieldLWWMerge`; `apply_record` `FieldLevelLWW` arms updated to use column-grain per-column HLC logic (read `_fed_sync_meta_cols`, run merge, write winning columns + updated column HLCs). Row-grain proxy comments removed. `PackedHLC` gains `PartialOrd`/`Ord` (field-declaration-order lexicographic, byte-identical to Swift `Comparable`). 11 unit tests (merge matrix, tombstone matrix, cross-leg golden) + 5 engine integration tests added. `cargo test` exit 0, 101 tests. | Completed 2026-07-17. | `packages/kits/ConvergenceKit/rust/src/federation.rs`; `packages/kits/ConvergenceKit/rust/src/record.rs`; `packages/kits/ConvergenceKit/rust/tests/field_lww_engine_tests.rs` | — |
| 10 | **Federation durable outbox/drain** | Federation uses an in-memory `pendingOutbound: [TableChange]` array rather than a durable SQLite outbox. Changes are lost on process death. For persistent cross-machine sync, Federation should adopt the same durable outbox pattern as the CloudKit backend (`_fed_outbox` table; `OutboxStore`). | Scope is large (new side table, migration, drain logic, echo suppression on restart); deferred as Federation's in-process relay is the current production target. | `packages/kits/ConvergenceKit/Sources/ConvergenceKitFederation/FederationSyncEngine.swift` (`pendingOutbound` array); `packages/kits/ConvergenceKit/Sources/ConvergenceKit/Outbox/OutboxStore.swift` | Tier-1 primitive-touching mission: add `_fed_outbox` durable side table; replace `pendingOutbound` with `OutboxStore`-backed drain; ensure echo suppression on reload (outbox entries are pre-stamped as `.local` origin). |
| 11 | **SyncValueBox depth hard-limit** | `SyncValueBox.array` is recursive — an array of arrays of arrays. No current enforcement bounds the nesting depth. A malformed inbound record could produce a deeply nested `SyncValueBox` that exhausts the stack on recursive decode. Defense in depth: cap decode depth at 3 levels (matching LocusKit's actual usage). | Advisory from Perkins review; no exploit vector in the current closed-channel Federation/CloudKit flow, but defense-in-depth is warranted. | `packages/kits/ConvergenceKit/Sources/ConvergenceKit/SyncRecord.swift` (`SyncValueBox` decode); `packages/kits/ConvergenceKit/rust/src/record.rs` (`SyncValueBox` serde decode) | Small additive mission: add `maxDepth: Int = 3` parameter to `SyncValueBox.fromJSON` (Swift) and a recursive depth counter to the Rust serde deserializer; add test that a 4-level array returns `.decodingFailure`. |

| 12 | ~~**A11 side-table consolidation: fold `_ck_device_identity` into `CKSideSchema`**~~ **DONE (CVK-WB12)** | **Shipped CVK-WB12:** `_ck_device_identity` declaration moved to `CKSideSchema` v9 (v8→v9 migration, additive IF NOT EXISTS). `DeviceIdentityStore.ensureSchema` delegates to `CKSideSchema.ensure` for call-site stability. All existing tests pass; one new consolidation-verification test added. SPEC B-12 and TRACKED_FOLLOWUPS updated. A11 consolidation is now complete — all `_ck_*` side tables are governed by a single `SchemaDeclaration`. | Completed 2026-07-17. | `packages/kits/ConvergenceKit/Sources/ConvergenceKit/SideSchema.swift`; `Sources/ConvergenceKit/DeviceRegistry/DeviceIdentityStore.swift` | — |

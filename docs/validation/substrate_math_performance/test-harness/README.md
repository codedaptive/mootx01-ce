# Test Harness

Cross-language conformance test harness for the GeniusLocus substrate
reference implementation. The harness generates and validates test
vectors against the scalar reference in both Swift and Rust, with
byte-equality (CRC32) as the conformance gate.

For the engineering catalog of all 24 primitives, see
[`primitive-catalog.md`](primitive-catalog.md). For the test
vector format specification, see
[`test-vector-format.md`](test-vector-format.md). For a worked
example of adding a new primitive, see
[`primitive-walkthrough-SimHash.md`](primitive-walkthrough-SimHash.md).

## Layout

```
test-harness/
├── README.md                            this file
├── primitive-catalog.md                 all 24 primitives, CRCs, references
├── test-vector-format.md                canonical vector file format spec
├── primitive-walkthrough-SimHash.md     worked example: adding a primitive
│
├── vectors/                             24 canonical conformance JSON files
│                                        (one per primitive, generated by Swift,
│                                        verified bit-identical against Rust)
│
├── rust/                         Rust harness (library + 4 binaries)
│   ├── Cargo.toml
│   ├── Cargo.lock
│   └── src/
│       ├── lib.rs
│       ├── bin/                  cargo bin targets
│       │   ├── gen_vectors.rs
│       │   ├── validate_vectors.rs
│       │   ├── stress_test.rs
│       │   └── topk_bench.rs
│       ├── harness/              shared harness library code
│       └── primitives/           per-primitive case generators + validators
│
└── swift/                        Swift package (library + 4 executables)
    ├── Package.swift
    ├── Sources/
    │   ├── GenVectors/           swift run gen-vectors
    │   ├── ValidateVectors/      swift run validate-vectors
    │   ├── StressTest/           swift run stress-test
    │   ├── TopKBench/            swift run topk-bench
    │   └── Harness/              shared harness library
    │       ├── Core/
    │       └── Primitives/       per-primitive case generators + validators
    └── Tests/
        └── HarnessTests/
```

## Directory naming conventions

The Rust and Swift subtrees follow each language's idiomatic naming.
Where the conventions differ, they are not normalized; doing so would
trigger compiler warnings or surprise readers familiar with the
language.

| Concept | Rust path | Swift path | Reason for difference |
|---|---|---|---|
| Library module / target | `rust/src/harness/` | `swift/Sources/Harness/` | Rust modules are lowercase snake_case (rustc lint enforced). Swift targets are PascalCase. |
| Per-primitive sources | `rust/src/primitives/` | `swift/Sources/Harness/Primitives/` | Same case rule. |
| Executable targets | `rust/src/bin/<name>.rs` | `swift/Sources/<Name>/` | Rust uses `src/bin/` flat files; Swift uses one directory per target under `Sources/`. |
| Source root | `rust/src/` | `swift/Sources/` | Each language's standard layout. |
| Build manifest | `rust/Cargo.toml` | `swift/Package.swift` | Each language's standard. |
| Tests | inline `#[cfg(test)]` in source files | `swift/Tests/HarnessTests/` | Rust's idiom is colocated tests; Swift's is a separate Tests/ root. |

## Running the harness

```sh
# Swift
cd swift
swift build
swift run gen-vectors --primitive simhash --seed 0xCAFEBABEDEADBEEF
swift run validate-vectors ../vectors/simhash.json

# Rust
cd rust
cargo build --release
cargo run --release --bin gen-vectors -- --primitive simhash --seed 0xCAFEBABEDEADBEEF --out /tmp/simhash-rust.json
cargo run --release --bin validate-vectors -- ../vectors/simhash.json
```

The conformance gate is CRC32 byte equality. A Swift-generated vector
file must validate in Rust and vice versa for every committed primitive.
CI (`.github/workflows/geniuslocus-conformance.yml`) runs the full
four-way matrix across all 24 primitives on every push and PR.

## Stress test

A third binary, `stress-test`, measures per-(op, batch_size, mode)
latency for the three batched kernel ops introduced in Phase 1 of
the kernel-learned-dispatch architecture. It is independent of
the conformance gate.

```sh
# Swift
cd swift
swift build -c release
.build/release/stress-test --out /tmp/stress-swift.json

# Rust
cd rust
cargo run --release --bin stress-test -- --out /tmp/stress-rust.json
```

The stress-test output is hardware-specific and is NOT committed
to the repo. The schema is documented in `test-vector-format.md`
under the stress-test entry.

## Top-K benchmark (historical Phase 2.delta-1 instrument)

A fourth binary, `topk-bench`, sweeps `hamming_top_k` latency
across K and N for every kernel in the registry. It is the
instrument that produced the Phase 2.delta-1 measurement cited
in row 5 of `docs/decisions/DECISION_PHASE_2_FINAL_SELECTION_2026-05-18.md`.
It is independent of the conformance gate, of the stress-test
sweep, and of the recurring CI matrix. It exists in both
languages so the cookbook section 17.1 hot-path budget for
`hamming_top_k` (K=10 over 1M rows under 100 microseconds) is
re-checkable from either side.

The Phase 2 dispatch question is closed (SimdKernel strictly
dominates on aarch64; see the final selection doc). The binaries
are retained as historical instruments rather than active sweep
participants: they are not invoked by CI, they do not contribute
to the recurring stress-test output, and the schema they emit
(`topk-1`) is parallel to but distinct from the stress-test
schema (`2`).

```sh
# Swift
cd swift
swift build -c release
.build/release/topk-bench --out /tmp/topk-swift.json

# Rust
cd rust
cargo run --release --bin topk-bench -- --out /tmp/topk-rust.json
```

### Baselines

The cookbook section 17.5 bandwidth floor for K=10 N=1M is
roughly 533 microseconds on apple-m5-max. SimdKernel sits 13%
above that floor; no kernel measured to date meets the section
17.1 budget of 100 microseconds. The cited number remains the
production answer.

| Run                       | Hardware     | Commit  | K=10 N=1M (microseconds) | Kernel               |
|---------------------------|--------------|---------|--------------------------|----------------------|
| Phase 2.delta-1           | apple-m5-max | 8e7916d | 604                      | Swift SimdKernel     |
| 2026-05-22                | apple-m5-max | 7e9efb9 | 594                      | Swift SimdKernel     |
| 2026-05-22 (pre-ladder)   | apple-m5-max | 7e9efb9 | 16,804                   | Rust SimdKernel      |
| 2026-05-22 (post-ladder)  | apple-m5-max | (this)  | 631                      | Rust SimdKernel      |
| 2026-05-22                | apple-m5-max | 7e9efb9 | 16,374                   | Rust ScalarKernel    |
| 2026-05-22                | apple-m5-max | 7e9efb9 | 29,044                   | Swift ScalarKernel   |
| 2026-05-22                | apple-m5-max | 7e9efb9 | 32,517                   | Swift MetalKernel    |
| 2026-05-22                | apple-m5-max | 7e9efb9 | 68,025                   | Swift BnnsKernel     |
| 2026-05-22                | apple-m5-max | 7e9efb9 | 166,470                  | Swift NeonKernel     |

Notes on the 2026-05-22 reading:

The Swift SimdKernel at 594 microseconds replicates the
Phase 2.delta-1 number (604 microseconds) within measurement
noise. No regression from the Mission 8 GeniusLocusKit work,
which is consistent with the harness having no transitive
dependency on the kit graph.

The Rust SimdKernel `hamming_top_k` initially measured at 16,804
microseconds: a 28x gap to the Swift counterpart. Inspection
showed the Rust implementation was score-then-sort with SIMD
distance, not the branchless-on-update ladder per cookbook
section 11.2. The source comment at the time labeled the ladder
as "Future work (Phase 2.beta-2)" but Phase 2 closed without it
landing. For a substrate intended as cross-system AI deep memory,
a 28x recall-path penalty on aarch64 Linux deployment targets
(Graviton, Ampere) was not viable. The ladder was ported from
the Swift implementation at
`packages/libs/SubstrateKernel/Sources/SubstrateKernel/PortableKernel.swift`
lines 113-211 into the Rust kernel at
`packages/libs/SubstrateKernel/rust/src/kernel_simd.rs` line 188. Post-port the Rust
SimdKernel measures 631 microseconds, sitting 18% above the
section 17.5 bandwidth floor of 533 microseconds and within 6%
of Swift. The conformance gate (byte-equality against
ScalarKernel) passes for both languages.

## Adding a primitive

Read [`primitive-walkthrough-SimHash.md`](primitive-walkthrough-SimHash.md)
end-to-end before starting. The catalog and the format spec are
authoritative; this README is orientation.
